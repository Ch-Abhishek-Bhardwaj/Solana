
Native solana

                            A ----------> B
                        ix = inc       ix = inc

Anchor client
                            A ------------> B
                         ix = inc        ix = inc

Interface defination language    Like an Abi(EVM)
when a program is deployed it generates an idl file